        {% if sidebar.show_page_menu %} <div class="p-4 rounded shadow-sm mb-4 notes-card">
        <div class="p-3"><h3 class="fw-bold mb-3">{{navbar.widget_title}}</h3>
        <ul class="row">{% for n in navbar.list %}
        <li class="col-6"><a href="{{n.url}}">{{n.nav}}</a></li>
        {% endfor %}</ul></div>
        </div>{% endif %}